# Production Environment Configuration
# This file uses GitLab CI/CD Variables for production deployment

# Server Configuration
HOSTNAME=observability-production
RESTART_POLICY=unless-stopped

# Network Configuration - From GitLab Variables
VM_IP=VM_IP
DOMAIN_ADDRESS=DOMAIN_ADDRESS
PROMETHEUS_SUB=metrics
GRAFANA_SUB=grafana
ALERTMANAGER_SUB=alerts
PUSHGATEWAY_SUB=pushgw
LOKI_SUB=loki

# tag image
PROMETHEUS_TAG=v3.4.1
GRAFANA_TAG=12.3.2
ALERTMANAGER_TAG=v0.29.0
PUSHGATEWAY_TAG=v1.12.0
CADVISOR_TAG=v0.50.0
NODE_EXPORTER_TAG=v1.10.1
BLACKBOX_TAG=v0.27.0
LOKI_TAG=3.3.2
PROMTAIL_TAG=3.3.2
TEMPO_SUB=2.7.2
MEMCACHED_TAG=1.6.38

# Authentication - From GitLab Variables (Masked)
GRAFANA_USERNAME=GRAFANA_USERNAME
GRAFANA_PASSWORD=GRAFANA_PASSWORD
# Recommended, signed Grafana plugins (safe defaults)
# You can pin versions with @x.y.z, e.g. yesoreyeram-infinity-datasource@2.6.5
GRAFANA_INSTALL_PLUGINS=yesoreyeram-infinity-datasource,marcusolsson-json-datasource,volkovlabs-echarts-panel,natel-plotly-panel,marcusolsson-dynamictext-panel,grafana-polystat-panel
## If you need to allow specific unsigned plugins, list IDs here (comma-separated)
## Example: grafana-exploretraces-app
GRAFANA_UNSIGNED_PLUGINS=

# SSL Configuration - From GitLab Variables
ACME_EMAIL=ACME_EMAIL

# Web Authentication - From GitLab Variables (Masked)
# Password should be generated: echo your_password | htpasswd -s -n -i username
WEB_AUTH_USER=WEB_AUTH_USER
WEB_AUTH_PASS=WEB_AUTH_PASS

# Slack Notifications - From GitLab Variables (Masked)
SLACK_WEBHOOK_URL=SLACK_WEBHOOK_URL
SLACK_CHANNEL=#alerts